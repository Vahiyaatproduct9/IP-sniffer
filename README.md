## IP sniffer using rust
### Usage:
- `-h` or `--help` to show help.
- `-j <number>` to use it parallely with multiple threads.
- `<IPAddress>` specify the IP Address to look for open ports.

### Use Case
```sh
cargo run -- -j 4 192.168.0.1
cargo run 192.168.0.1
```

### Thank You for visiting...
